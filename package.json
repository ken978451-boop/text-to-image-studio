{
  "name": "text-to-image-studio",
  "version": "1.0.0",
  "private": true,
  "description": "A privacy-first local OpenAI Image API example for turning text prompts into images.",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "5.2.1",
    "express-rate-limit": "8.6.2",
    "helmet": "8.3.0",
    "openai": "7.4.0"
  }
}
